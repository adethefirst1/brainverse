# Netlify build configuration for Flutter web (BrainVerse puzzle_games)
# Uses netlify-plugin-flutter to install Flutter and build the app.

[[plugins]]
  package = "netlify-plugin-flutter"

[plugins.inputs]
  channel = "stable"

[build]
  command = "flutter build web --release"
  publish = "build/web"
